<%# Reusable ticket-level detail table. Each render owns its controls, rows and expand modal; %>
<%# sla_dashboard_detail_table.js scopes every interaction to data-sla-detail-instance. %>
<% table_key = local_assigns.fetch(:table_key).to_s %>
<% detail_results = local_assigns.fetch(:results) %>
<% detail_counts = local_assigns.fetch(:counts) %>
<% include_resolution = local_assigns.fetch(:include_resolution, false) %>
<% show_title = local_assigns.fetch(:show_title, true) %>
<% card_classes = local_assigns.fetch(:card_classes, 'tw-flex tw-flex-col tw-min-w-0') %>
<% initial_state = table_key == 'open' ? @state_filter : 'all' %>
<% initial_search = table_key == 'open' ? @search_query : nil %>
<% prefix = table_key == 'open' ? 'sla-detail' : "sla-#{table_key}-detail" %>
<% detail_columns = [
     ['ticket', l(:field_sla_detail_ticket), 'number', false],
     ['project', l(:field_sla_detail_project), 'text', true],
     ['tracker', l(:field_sla_detail_tracker), 'text', true],
     ['title', l(:field_sla_detail_title), 'text', false],
     ['status', l(:field_sla_detail_status), 'number', true],
     ['assignee', l(:field_sla_detail_assignee), 'text', false],
     ['first_response', l(:field_sla_detail_first_response), 'number', true]
   ] %>
<% detail_columns << ['resolution', l(:field_sla_detail_resolution), 'number', true] if include_resolution %>
<% detail_columns.concat([
     ['result', l(:field_sla_detail_result), 'number', false],
     ['deviation', l(:field_sla_detail_deviation), 'number', false]
   ]) %>
<% detail_per_page_options = ([10] + Setting.per_page_options_array).uniq %>

<div id="<%= prefix %>-card" data-sla-detail-instance="<%= table_key %>"
     class="<%= card_classes %>">
  <div class="tw-flex tw-items-start tw-justify-between tw-gap-3 tw-mb-4">
    <div class="tw-flex tw-items-center tw-gap-3 tw-flex-wrap">
      <% if show_title %>
        <h3 class="tw-m-0 tw-p-0 tw-text-base tw-font-semibold tw-text-gray-900 tw-whitespace-nowrap"><%= l(:label_sla_detail_table) %></h3>
      <% end %>
      <div id="<%= prefix %>-state-tabs" data-sla-detail-state-tabs class="tw-flex tw-flex-wrap tw-gap-2">
        <%= render 'sla_dashboard/state_tabs', counts: detail_counts,
              state_filter: initial_state, table_key: table_key %>
      </div>
    </div>

    <button type="button" data-sla-detail-expand
            title="<%= l(:label_sla_detail_expand_title) %>" aria-label="<%= l(:label_sla_detail_expand) %>"
            class="tw-flex-shrink-0 tw-inline-flex tw-items-center tw-justify-center tw-w-10 tw-h-10 tw-bg-white tw-text-gray-500 tw-border tw-border-gray-300 tw-rounded-lg tw-shadow-none hover:tw-bg-gray-50 hover:tw-text-gray-700 focus:tw-outline-none focus:tw-ring-4 focus:tw-ring-primary-100">
      <svg class="tw-w-5 tw-h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
        <polyline points="15 3 21 3 21 9"></polyline>
        <polyline points="9 21 3 21 3 15"></polyline>
        <line x1="21" y1="3" x2="14" y2="10"></line>
        <line x1="3" y1="21" x2="10" y2="14"></line>
      </svg>
    </button>
  </div>

  <div id="<%= prefix %>-table-home" data-sla-detail-home class="tw-flex-1 tw-min-h-0">
    <div id="<%= prefix %>-table" data-sla-detail-table
         data-l-no-search-match="<%= l(:text_sla_detail_no_search_matches) %>"
         data-l-empty="<%= l(:text_sla_detail_table_empty_state) %>">
      <div data-sla-detail-controls class="tw-items-center tw-flex-wrap tw-gap-4">
        <div class="tw-relative">
          <svg class="tw-w-4 tw-h-4 tw-absolute tw-left-3 tw-top-1/2 -tw-translate-y-1/2 tw-text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-4.35-4.35M11 19a8 8 0 100-16 8 8 0 000 16z" />
          </svg>
          <%= search_field_tag "#{table_key}_q", initial_search,
                id: "#{prefix}-search", placeholder: l(:label_sla_detail_search_placeholder),
                autocomplete: 'off', data: { 'sla-detail-search': true },
                class: 'tw-bg-white tw-border tw-border-gray-300 tw-text-gray-900 tw-text-sm tw-rounded-lg ' \
                       'focus:tw-ring-primary-500 focus:tw-border-primary-500 tw-pl-10 tw-pr-3 tw-py-2.5 tw-w-80 tw-max-w-full' %>
        </div>

        <div class="tw-flex tw-items-center tw-gap-2 tw-text-sm tw-text-gray-500">
          <label for="<%= prefix %>-perpage-trigger" class="tw-whitespace-nowrap"><%= l(:label_sla_detail_per_page) %></label>
          <button type="button" id="<%= prefix %>-perpage-trigger"
                  data-sla-perpage-trigger data-dropdown-toggle="<%= prefix %>-perpage-menu"
                  class="tw-inline-flex tw-items-center tw-gap-1.5 tw-bg-white tw-text-gray-900 tw-border tw-border-gray-300 tw-text-sm tw-rounded-lg tw-py-2 tw-pl-3 tw-pr-2.5 tw-shadow-none hover:tw-bg-gray-50 focus:tw-outline-none focus:tw-ring-4 focus:tw-ring-primary-100">
            <span data-sla-perpage-label><%= detail_per_page_options.first %></span>
            <svg class="tw-w-3 tw-h-3 tw-text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div id="<%= prefix %>-perpage-menu" data-sla-perpage-menu
               class="hidden tw-z-10 tw-bg-white tw-border tw-border-gray-200 tw-divide-y tw-divide-gray-100 tw-rounded-lg tw-shadow-lg tw-w-20">
            <ul class="tw-list-none tw-m-0 tw-py-1 tw-pl-0 tw-text-sm tw-text-gray-700">
              <% detail_per_page_options.each do |n| %>
                <li>
                  <button type="button" data-sla-perpage-option="<%= n %>"
                          class="tw-block tw-w-full tw-text-left tw-bg-white tw-text-gray-700 tw-border-0 tw-shadow-none tw-px-4 tw-py-2 hover:tw-bg-gray-50 hover:tw-text-gray-900 focus:tw-outline-none"><%= n %></button>
                </li>
              <% end %>
            </ul>
          </div>
          <%= select_tag "#{table_key}_detail_per_page",
                options_for_select(detail_per_page_options, detail_per_page_options.first),
                id: "#{prefix}-per-page", data: { 'sla-perpage': true }, class: 'tw-hidden' %>
        </div>
      </div>

      <div data-sla-detail-tablewrap>
        <table class="tw-min-w-full tw-text-sm tw-text-left">
          <thead>
            <tr class="tw-border-b tw-border-gray-200 tw-text-gray-500">
              <% detail_columns.each do |key, label, type, extra| %>
                <th data-sla-sort="<%= key %>" data-sla-sort-type="<%= type %>"
                    class="tw-py-2 tw-pr-4 tw-font-medium tw-whitespace-nowrap tw-cursor-pointer tw-select-none<%= ' sla-detail-col-extra' if extra %>">
                  <span class="tw-inline-flex tw-items-center tw-gap-1 tw-text-gray-500 hover:tw-text-gray-700">
                    <%= label %>
                    <span class="sla-sort-icon tw-inline-flex tw-text-gray-400" aria-hidden="true"></span>
                  </span>
                </th>
              <% end %>
            </tr>
          </thead>
          <tbody id="<%= prefix %>-table-body" data-sla-detail-tbody>
            <% detail_results.each do |sla_result| %>
              <% issue = sla_result.issue %>
              <% effective_state = sla_result.effective_primary_state %>
              <% effective_deviation = sla_result.effective_deviation_seconds %>
              <% result_label = sla_card_label(effective_state.to_sym) %>
              <% result_label = "#{result_label} #{l(:label_sla_card_at_risk)}" if sla_result.effective_at_risk? %>
              <% search_terms = [issue.id, issue.project.name, issue.tracker.name, issue.subject,
                                 issue.status.name, issue.assigned_to&.name, result_label].join(' ') %>
              <% response_duration = sla_result.completed_response_seconds %>
              <% resolution_duration = sla_result.completed_resolution_seconds %>
              <tr data-sla-row data-sla-search="<%= search_terms %>"
                  data-sla-state="<%= effective_state %>"
                  data-sla-at-risk="<%= sla_result.effective_at_risk? %>"
                  class="tw-border-b tw-border-gray-100 hover:tw-bg-gray-50"
                  id="<%= prefix %>-row-<%= issue.id %>">
                <td class="tw-py-2.5 tw-pr-4" data-sla-sort-value="<%= issue.id %>">
                  <%= link_to "##{issue.id}", issue_path(issue), target: '_blank', rel: 'noopener',
                        class: 'tw-text-primary-600 tw-font-medium hover:tw-underline' %>
                </td>
                <td class="tw-py-2.5 tw-pr-4 sla-detail-col-extra"><%= issue.project.name %></td>
                <td class="tw-py-2.5 tw-pr-4 sla-detail-col-extra"><%= issue.tracker.name %></td>
                <td class="tw-py-2.5 tw-pr-4 tw-truncate sla-detail-title">
                  <%= link_to issue.subject, issue_path(issue), target: '_blank', rel: 'noopener',
                        class: 'tw-text-gray-800 hover:tw-underline' %>
                </td>
                <td class="tw-py-2.5 tw-pr-4 sla-detail-col-extra" data-sla-sort-value="<%= issue.status.position %>"><%= issue.status.name %></td>
                <td class="tw-py-2.5 tw-pr-4"><%= issue.assigned_to&.name %></td>
                <td class="tw-py-2.5 tw-pr-4 tw-whitespace-nowrap sla-detail-col-extra" data-sla-sort-value="<%= response_duration %>"><%= response_duration.present? ? format_sla_duration(response_duration) : '—' %></td>
                <% if include_resolution %>
                  <td class="tw-py-2.5 tw-pr-4 tw-whitespace-nowrap sla-detail-col-extra" data-sla-sort-value="<%= resolution_duration %>"><%= resolution_duration.present? ? format_sla_duration(resolution_duration) : '—' %></td>
                <% end %>
                <td class="tw-py-2.5 tw-pr-4 tw-whitespace-nowrap" data-sla-sort-value="<%= sla_result_sort_rank(sla_result) %>">
                  <span class="tw-inline-block tw-rounded-full tw-px-2 tw-py-0.5 tw-text-xs tw-font-medium <%= sla_card_color_classes(effective_state.to_sym) %>">
                    <%= sla_card_label(effective_state.to_sym) %>
                  </span>
                  <% if sla_result.effective_at_risk? %>
                    <span class="tw-inline-block tw-rounded-full tw-px-2 tw-py-0.5 tw-text-xs tw-font-medium <%= sla_card_color_classes(:at_risk) %>">
                      <%= l(:label_sla_card_at_risk) %>
                    </span>
                  <% end %>
                </td>
                <td class="tw-py-2.5 tw-pr-4 tw-whitespace-nowrap <%= 'tw-text-red-600 tw-font-medium' if effective_deviation.present? %>"
                    data-sla-sort-value="<%= effective_deviation %>">
                  <%= effective_deviation.present? ? "+#{format_sla_duration(effective_deviation)}" : '—' %>
                </td>
              </tr>
            <% end %>
            <% if detail_results.empty? %>
              <tr data-sla-empty-row>
                <td colspan="<%= detail_columns.size %>" class="tw-py-8 tw-text-center tw-text-gray-400">
                  <%= l(:text_sla_detail_table_empty_state) %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>

      <div id="<%= prefix %>-pagination" data-sla-detail-pagination
           data-l-showing="<%= l(:label_sla_detail_showing) %>" data-l-of="<%= l(:label_sla_detail_of) %>"
           data-l-prev="<%= l(:label_previous) %>" data-l-next="<%= l(:label_next) %>"
           class="tw-mt-4 tw-flex tw-items-center tw-justify-between tw-flex-wrap tw-gap-3 tw-text-sm tw-text-gray-500"></div>
    </div>
  </div>

  <div id="<%= prefix %>-modal" data-sla-detail-modal
       class="hidden tw-fixed tw-inset-0 tw-z-50 tw-flex tw-items-center tw-justify-center tw-p-4"
       role="dialog" aria-modal="true" aria-labelledby="<%= prefix %>-modal-title">
    <div class="tw-absolute tw-inset-0 tw-bg-gray-900/50" data-sla-detail-close></div>
    <div class="tw-relative tw-bg-white tw-rounded-xl tw-shadow-xl tw-w-full tw-max-w-[92vw] tw-h-[88vh] tw-flex tw-flex-col">
      <div class="tw-flex tw-items-center tw-justify-between tw-gap-3 tw-px-5 tw-py-4 tw-border-b tw-border-gray-200">
        <h3 id="<%= prefix %>-modal-title" class="tw-m-0 tw-p-0 tw-text-base tw-font-semibold tw-text-gray-900"><%= l(:label_sla_detail_table) %></h3>
        <button type="button" data-sla-detail-close aria-label="<%= l(:button_close) %>"
                class="tw-inline-flex tw-items-center tw-justify-center tw-w-9 tw-h-9 tw-bg-gray-100 tw-text-gray-600 tw-border-0 tw-rounded-full tw-shadow-none hover:tw-bg-gray-200 hover:tw-text-gray-900 focus:tw-outline-none focus:tw-ring-4 focus:tw-ring-primary-100">
          <svg class="tw-w-6 tw-h-6" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
            <path d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
      <div class="tw-flex tw-items-center tw-justify-between tw-flex-wrap tw-gap-3 tw-px-5 tw-py-3 tw-border-b tw-border-gray-200">
        <div data-sla-modal-pills class="tw-flex tw-flex-wrap tw-gap-2">
          <%= render 'sla_dashboard/state_tabs', counts: detail_counts,
                state_filter: initial_state, table_key: table_key %>
        </div>
        <div data-sla-modal-controls-slot class="tw-flex tw-items-center tw-flex-wrap tw-gap-4"></div>
      </div>
      <div data-sla-detail-modal-body class="tw-p-5 tw-flex-1"></div>
    </div>
  </div>
</div>
