<h2><%= link_to l(:label_sla_target_options), sla_target_options_path %> &#187; <%= l(:label_sla_target_option_new) %></h2>

<%= labelled_form_for @target_option, url: sla_target_options_path do |f| %>
  <%= render 'form', f: f %>
  <p><%= submit_tag l(:button_create) %></p>
<% end %>
